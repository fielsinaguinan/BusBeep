
  # BusBeep

  This is a code bundle for BusBeep. The original project is available at https://www.figma.com/design/ezRm7glzmOvgiCtRVSofC7/BusBeep.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  